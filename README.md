# PayFlow-
PayFlow - это высокопроизводительная распределенная платежная платформа, построенная на микросервисной архитектуре. Система обеспечивает обработку транзакций в реальном времени с гарантированной доставкой и полным соответствием финансовым регуляциям.

Полнофункциональная система обработки платежей с поддержкой транзакций, валидации, логирования и аналитики.

payment-system/
├── payment_system.py          # Основное приложение (моделей, сервисы, API)
├── test_payment_system.py      # Тесты (pytest)
├── requirements.txt            # Зависимости Python
├── docker-compose.yml          # Конфигурация Docker
├── .env.example               # Пример конфигурации окружения
├── README.md                  # Этот файл
├── docs/
│   ├── architecture.md        # Архитектура системы
│   ├── database_schema.md     # Схема БД
│   └── api_documentation.md   # API документация
└── scripts/
    ├── init_db.py             # Инициализация БД
    └── seed_data.py           # Заполнение тестовых данных

🚀 Быстрый старт

# Клонируем проект
git clone https://github.com/user/payment-system.git
cd payment-system

# Создаем файл .env
cp .env.example .env

# Запускаем контейнеры
docker-compose up -d

# Проверяем статус
docker-compose ps

# Логи
docker-compose logs -f app

Доступные сервисы:

API: http://localhost:8000

Swagger UI: http://localhost:8000/docs

ReDoc: http://localhost:8000/redoc

pgAdmin: http://localhost:5050

Вариант 2: Локальная установка
Предусловия
Python 3.11+

PostgreSQL 13+

Redis 6.0+ (опционально)

Установка

# 1. Создаем виртуальное окружение
python -m venv venv

# Активируем
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# 2. Устанавливаем зависимости
pip install -r requirements.txt

# 3. Создаем .env файл
cp .env.example .env

# Отредактируйте .env с вашей конфигурацией БД:
# DATABASE_URL=postgresql://user:password@localhost/payment_system

# 4. Инициализируем БД
python -c "from payment_system import Base, engine; Base.metadata.create_all(bind=engine)"

# 5. Запускаем приложение
uvicorn payment
